# Niriksh — The Evidence Chain

Seven seconds, 1920 × 1080, 60 fps. The supplied SVG remains unchanged in `public/intro/niriksh-logo.svg`; separate viewports reveal its emblem and original lettering.

0–1.5s: scattered drifting evidence graph.
1.5–3s: central pulse and honeycomb alignment.
3–5s: emblem expands, lattice moves outward, charcoal resolves into cream.
5–7s: original wordmark fades in and holds.

Preview: `npx remotion studio remotion/index.tsx --no-open --port=3100`

Export: `npx remotion render remotion/index.tsx EvidenceChain output/intro/niriksh-evidence-chain.mp4 --codec=h264 --crf=16`

The clip is silent for mixing with demo narration/music. All movement is frame-driven and deterministic. The final cream background preserves the supplied logo's dark lettering and original colors.
